defmodule ComponentsGuide.Wasm.Examples.HTTPHarness do
  # Takes another WebAssembly module as imports, who has the role of a HTTP server.
  # This module lets you validate things like HTTP caching headers e.g. etag and date modified.
end
